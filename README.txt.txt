Project can be run in NetBeans.

Use "admin" and "pass" for username and password respectevly to log in in owner mode.

Use "user" and "pass" or simply register as a new user to log in in customer mode.